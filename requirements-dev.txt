# Dependências de desenvolvimento — não incluídas no .exe
# Instalar: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.2.0
pytest-cov==5.0.0
pyinstaller==6.6.0
fonttools==4.63.0  # usado apenas por scripts/build_fonts.py (gera assets/fonts/*.ttf)
